[package]
name = "volkrix-nnue"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde_json = "1.0"
volkrix = { path = "../..", features = ["offline-tools"] }
